#### rmq_setup.py
Ќабор утилит дл€ настройки RabbitMQ. Cоздать/удалить очередь/exhange/binding и.т.д см. 
```bash
python rmq_setup.py -h
```

#### rmq_publish.py
”тилита дл€ публикации сообщений в RabbitMQ. “ело сообщени€ может быть передано в качестве параметра при запуске,
либо размещено в файле *.txt с указанием ссылки на него при запуске. ќстальные параметры передаютс€ призапуске в €вном виде.

#### rmq_consume.py
”тилита осуществл€юща€ вычитку сообщений из очереди. ¬озможна вычитка из уже существующей очереди,
либо создание временной, котора€ будет удалена по окончании работы.

«апускаем из командной строки
{ѕуть до питона, либо просто python если путь уже прописан в PATH} rmq_setup.py -h
и читаем справку, там все пон€тно. —правка доступна как дл€ скрипта в целом, так и дл€ каждой отдельной функции

```bash
python rmq_setup.py command -h
```

¬ файле **rmq_conmmon_tools.py** прописаны строчки дл€ конекта к кролику и общие методы, его выполн€ть не нужно))